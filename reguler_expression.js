// pattern regex
const regex1 = /pattern/; // 'pattern' adalah pola teks yang ingin dicocokkan

// constructor regex
const regex2 = new RegExp('pattern');

// contoh
const regexLiteral = /hello/;  
const regexConstructor = new RegExp('hello');

